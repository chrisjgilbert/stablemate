<%#
  Logo + ComingSoonBadge pair shown above the heading on every signed-out auth
  screen (sign in, sign up, forgot/reset password) — these don't share the
  authenticated app header, so the badge rides along with the logo instead.
%>
<%= render "shared/logo", size: 40 %>
<%= render "shared/coming_soon_badge" %>
