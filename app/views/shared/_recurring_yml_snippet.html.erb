<%#
  The gem's config/recurring.yml example — the "connect the gem" code fence shared
  by the zero-project first-run state and the no-monitors empty state, so the two
  never drift.
%>
<pre class="rounded-lg bg-warmdark text-surface-subtle p-4 font-mono text-[12px] overflow-x-auto"><code># config/recurring.yml
daily_digest:
  class: DailyDigestJob
  schedule: every day at 9am</code></pre>
