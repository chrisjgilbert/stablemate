<%= @monitor.name %> is down.

<%= @monitor.name %> missed its expected check-in.<% if @expected_by %> No ping arrived by <%= @expected_by.utc.strftime("%Y-%m-%d %H:%M UTC") %>.<% end %> Check your job logs to see what happened.

View monitor: <%= monitor_url(@monitor) %>
