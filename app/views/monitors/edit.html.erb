<% content_for :title, "Edit monitor" %>
<div class="max-w-xl mx-auto">
  <h1 class="text-xl font-semibold tracking-tight mb-6">Edit <span class="font-mono"><%= @monitor.name %></span></h1>
  <div class="rounded-xl border border-border bg-surface p-6 shadow-sm">
    <%= render "monitors/form", monitor: @monitor %>
  </div>
</div>
