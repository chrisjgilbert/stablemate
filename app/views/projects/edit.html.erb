<% content_for :title, "Project settings" %>

<div class="max-w-md mx-auto space-y-10">
  <div>
    <div class="flex items-center gap-3 mb-6">
      <%= link_to "Projects", projects_path, class: "text-[13px] text-muted hover:text-ink" %>
      <span class="text-muted">/</span>
      <h1 class="font-display text-xl font-bold tracking-tight">Settings</h1>
    </div>
    <%= render "projects/form", project: @project %>
  </div>

  <%= render "projects/danger_zone", project: @project %>
</div>
