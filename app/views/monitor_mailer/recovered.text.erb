<%= @monitor.name %> is back up.

<% if @monitor.last_ping_at %>A ping arrived at <%= @monitor.last_ping_at.utc.strftime("%Y-%m-%d %H:%M UTC") %>. <% end %>Your job is running again.

View monitor: <%= monitor_url(@monitor) %>
